import { makeStyles } from '@material-ui/core';

const styles = makeStyles(() => ({
    root: {
        position: 'absolute',
        top: 75,
        right: 20,
        fontWeight: 600,
        backgroundColor: '#5aff78',
    },
    label: {
        color: '#525252',
    },
    icon: {
        color: '#525252',
    },
}));

export default styles;
